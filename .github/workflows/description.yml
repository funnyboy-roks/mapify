name: modrinth-desc

on:
  push:
    branches: [ main ]
    paths: [ 'README.md' ]
jobs:
  modrinth-desc:
    runs-on: 'ubuntu-latest'
    steps:
    - uses: actions/checkout@v3
    - uses: naineel1209/modrinth-auto-desc@feat/add-relative-path-support
      with:
        auth-token: ${{ secrets.MODRINTH_AUTH_TOKEN }}
        slug: 'mapify'
